# Desktop with Devcontainer

This example shows how to setup a remote Desktop interface with a container running with devcontainer.

- The desktop is accesible with either a VNC client or the web browser.
- Password is `vscode`

