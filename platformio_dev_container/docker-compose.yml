version: '3'
services:
  devenv:
    build:
      context: .
      dockerfile: Dockerfile # The path to our Dockerfile
    command: /bin/bash
    devices:
      - "/dev/bus/usb/"
    tty: true
    #volumes:
    #  - /dev/bus/usb:/dev/bus/usb
